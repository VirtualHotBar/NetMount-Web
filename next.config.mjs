/** @type {import('next').NextConfig} */
const nextConfig = {
/*     i18n:{
        locales:['zh','en'],
        defaultLocale:'zh',
    } */
   images:{
       domains:['p1.hotpe.top']
   }
};

export default nextConfig;
